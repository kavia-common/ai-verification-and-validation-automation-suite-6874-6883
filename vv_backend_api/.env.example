# Directory for persistent data: DB file, uploads, scripts, run artifacts
DATA_DIR=./data

# Optional database URL (SQLite used by default if not set)
# DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname

# LLM configuration
LLM_PROVIDER=mock
LLM_MOCK=true
# LLM_API_KEY=your_key_if_using_real_provider
