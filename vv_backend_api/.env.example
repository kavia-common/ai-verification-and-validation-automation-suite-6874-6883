# Environment variables for vv_backend_api

# DATABASE_URL: Optional. If set, the backend connects to this database (e.g., Postgres).
# Example for Postgres:
# DATABASE_URL=postgresql+psycopg2://USER:PASS@HOST:PORT/DBNAME
# If not set, the app will default to SQLite stored under DATA_DIR/app.db
# DATABASE_URL=

# DATA_DIR: Directory for local data (SQLite database location by default).
# Defaults to ./data if not provided.
DATA_DIR=./data
